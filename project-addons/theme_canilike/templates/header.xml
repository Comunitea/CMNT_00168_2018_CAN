<?xml version="1.0" encoding="utf-8" ?>
<odoo>

     <!-- Set Up Responsive Logo Show -->
    <template id="can_logo_show" name="Logo Show Canilike Template" inherit_id="website_multi_theme.auto_view_can_website_layout_logo_show">
        <xpath expr="//a[hasclass('navbar-brand')]/span" position="replace">
<!--            Not logo -->
<!--            <h1>CAN I LIKE</h1>-->
<!--            <p>Tradition canning updated</p>-->
<!--            with logo -->
            <figure>
                <img class="img img-responsive" src="/theme_canilike/static/img/canilike/img-logo.png" alt="website.name"/>
                <figcaption><h1>CAN I LIKE</h1></figcaption>
                <figcaption><p>Tradition canning updated</p></figcaption>
            </figure>
        </xpath>
    </template>

    <template id="can_website_layout" name="Main layout Canilike" inherit_id="website.layout">

        <!-- Replace original header submenus -->
        <xpath expr="//t[@t-foreach='website.menu_id.child_id']" position="replace"/>

        <xpath expr="//div[hasclass('navbar', 'navbar-default', 'navbar-static-top')]" position="after">
            <!-- Include shop search box in header keeping category if exist -->
            <div id="header_search_box">
                <t t-if="xmlid == 'website_sale.products'">
                    <form method="get" t-att-class="search_class"
                          t-att-action="'/category/%s' % category.slug if category and category.slug else keep('/shop'+ ('/category/'+slug(category)) if category else None, search=0)">
                        <t t-if="attrib_values">
                            <t t-foreach="attrib_values" t-as="a">
                                <input type="hidden" name="attrib" t-att-value="'%s-%s' % (a[0], a[1])" />
                            </t>
                        </t>
                        <t t-call="website.website_search_box" />
                    </form>
                </t>
                <t t-else="">
                    <form action="/shop" method="get" class="navbar-search pull-left pagination form-inline">
                        <t t-call="website.website_search_box">
                            <t t-set="search_class" t-value="'pagination form-inline o_website_sale_search'"/>
                        </t>
                    </form>
                </t>
            </div>

            <!-- Add header submenus -->
            <div id="header_submenu">
                <div class="navbar-default navbar-static-top">
                    <div class="collapse navbar-collapse navbar-top-collapse">
                        <ul class="nav navbar-nav navbar-right" id="top_submenu">
                            <t t-foreach="website.menu_id.child_id" t-as="submenu">
                                <t t-call="website.submenu"/>
                            </t>
                        </ul>
                    </div>
                </div>
            </div>
        </xpath>
    </template>

</odoo>